/**
 * ChainPickerScreen tests — SETUP-01
 * Stubs: real tests added after WalletSetupScreen is updated in Plans 09-01/09-02.
 */

describe("ChainPickerScreen — chain picker populated from blockchains.json", () => {
  it.todo(
    "renders only enabled blockchains from BLOCKCHAINS export (enabled: true filter)",
  );

  it.todo("renders only enabled networks for the selected blockchain");

  it.todo("selecting a chain updates NewWalletContext blockchainConfig");
});
